# Create DevCycle App
A bash script used to generate a DevCycle example app

Example Usage:
```bash
bash create.sh
```

```bash
bash create.sh \
    -o ./devcycle-onboarding-example \
    -t react-typescript \
    -k client-sdk-key
```
